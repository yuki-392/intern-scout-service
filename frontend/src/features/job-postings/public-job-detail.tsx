"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { applyToJobPosting, getPublicJobPosting } from "./job-posting-api";
import type { JobPosting } from "./job-posting-types";
import styles from "./job-postings.module.css";
export function PublicJobDetail({ id }: { id: number }) { const [job, setJob] = useState<JobPosting | null>(null); const [state, setState] = useState<"loading" | "ready" | "confirm" | "applied" | "success" | "not-found">("loading"); const [conversation, setConversation] = useState<number | null>(null); useEffect(() => { void getPublicJobPosting(id).then((result) => { setJob(result); setState(result.applied ? "applied" : "ready"); }).catch((error) => setState(error?.status === 404 ? "not-found" : "loading")); }, [id]); if (state === "not-found") return <p>募集が見つかりません</p>; if (!job) return <p>募集を読み込んでいます</p>; async function apply() { const result = await applyToJobPosting(id); setConversation(result.conversation_id); setState("success"); } return <article className={styles.panel}><h1>{job.title}</h1><p>{job.company_name}</p><p>{job.description}</p><p>{job.work_conditions}</p>{state === "ready" && <button className={styles.button} onClick={() => setState("confirm")}>応募する</button>}{state === "confirm" && <div><p>募集『{job.title}』に応募しました。プロフィールをご確認ください。が企業へ送信されます</p><button className={styles.button} onClick={() => void apply()}>応募を確定</button></div>}{state === "applied" && <p>応募済み</p>}{state === "success" && <p>応募しました</p>}{conversation && <Link href={`/conversations/${conversation}`}>会話を開く</Link>}</article>; }
