import { CompanyJobList } from "../../../features/job-postings/company-job-list";
import styles from "../../../features/intern-search/intern-search.module.css";
export default async function CompanyJobsPage({ searchParams }: { searchParams: Promise<{ created?: string; page?: string }> }) { const values = await searchParams; const created = values.created ?? "initial"; const parsedPage = Number.parseInt(values.page ?? "1", 10); const page = parsedPage > 0 ? parsedPage : 1; return <main className={styles.shell}><div className={styles.content}><h1>自社募集</h1>{created !== "initial" ? <p role="status">募集を作成しました</p> : null}<CompanyJobList page={page} refreshKey={created} /></div></main>; }
